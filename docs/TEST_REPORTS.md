# 测试报告

## DeepSeek 接入与 403 排查 · 2026-09-10

- 同一港股行情请求直连返回403，通过本机HTTP代理返回200，已配置本地 DATA_PROXY_URL，重测0700.HK获得491根日线。
- 单元测试14/14通过，新增模型成功、无Key、HTTP失败、无效JSON、非法引用和交易建议拒绝测试；模型接口使用模拟响应，未进行真实模型调用。
- DeepSeek解读与规则事实分开展示，行情失败不调用模型，模型超时或校验失败不影响规则结果。引用存在性与关键词检查不是完整的语义核验。
- 本机 .env 已确认被Git忽略；需用户填入新的有效 DEEPSEEK_API_KEY 后重启。未复用聊天中暴露的Key。

## v1.0 · 2026-09-10

- Node 单元测试 10/10 通过：SMA/EMA、平价无交叉、单调趋势、交易日收益、数据不足、金叉死叉前后关系、量能分母不含当日、无未来数据污染、市场代码与无效输入、交易所当地日期及复权、异常源数据。
- 真实数据验证：600519.SS 返回 484 根、AAPL 返回 501 根、0700.HK 返回 491 根有效日线；截至日期均为 2026-09-09。使用本机 HTTP 代理访问 Yahoo，不能据此承诺其他环境或全部证券都可用。
- Chromium 端到端：三市场输入、公司身份显示、SVG 图表、点击事件查看证据、6个月范围、JSON下载、390px移动端无横向溢出、非法输入错误反馈，全部通过，无页面脚本错误。
- 已人工查看真实结果截图，未调用模型或执行交易。
- 限制：没有完整交易所日历；当日线一律保守排除。尚未进行授权数据源交叉对账、长期回测或性能压测，不提供胜率与收益预测。
